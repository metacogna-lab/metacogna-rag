# MetaCogna RAG Environment Variables
# Copy this file to .env.production for production deployment

# =============================================================================
# FRONTEND API KEYS (Vite Environment Variables)
# =============================================================================
# NOTE: Keys prefixed with VITE_ are exposed to the client bundle at build time.
# Use these ONLY for development/testing. For production, use Worker secrets.

# Google Gemini API Key (for frontend LLM functionality)
# Get your API key from: https://makersuite.google.com/app/apikey
# VITE_GEMINI_API_KEY=your_google_gemini_api_key_here

# OpenAI API Key (for frontend LLM functionality)
# Get your API key from: https://platform.openai.com/api-keys
# VITE_OPENAI_API_KEY=your_openai_api_key_here

# Anthropic API Key (for frontend LLM functionality)
# Get your API key from: https://console.anthropic.com/
# VITE_ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Cloudflare Workers AI Token (optional, for frontend Workers AI access)
# VITE_WORKERS_AI_TOKEN=your_workers_ai_token_here

# =============================================================================
# BACKEND VARIABLES (Cloudflare Worker Environment)
# =============================================================================
# Backend environment variables are configured via Cloudflare dashboard or wrangler.toml
# NOT in this .env file. Set Worker secrets using:
# bun wrangler secret put GEMINI_API_KEY
# bun wrangler secret put OPENAI_API_KEY
# bun wrangler secret put ANTHROPIC_API_KEY

# =============================================================================
# OPTIONAL LOCAL DEVELOPMENT VARIABLES
# =============================================================================

# ChromaDB URL (Optional - defaults to http://localhost:8000 if not set)
# Used for vector storage in local development with Docker Compose
# CHROMA_URL=http://localhost:8000

# =============================================================================
# LANGFUSE OBSERVABILITY (Optional)
# =============================================================================
# Langfuse is used for LLM observability and tracing
# Sign up at: https://cloud.langfuse.com

# Langfuse API host
LANGFUSE_HOST=https://cloud.langfuse.com

# Langfuse public key (get from Langfuse dashboard)
LANGFUSE_PUBLIC_KEY=pk-lf-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Langfuse secret key (get from Langfuse dashboard)
LANGFUSE_SECRET_KEY=sk-lf-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# =============================================================================
# CLOUDFLARE BINDINGS (Configured in wrangler.toml)
# =============================================================================
# These are NOT environment variables but Cloudflare Worker bindings:
# - AI: Workers AI binding for embeddings and LLM inference
# - VECTOR_INDEX: Vectorize binding for vector search (index: pratejra-index)
# - DB: D1 Database binding for relational storage (database: pratejra-db)
#
# Setup commands:
# 1. Create D1 database: bun wrangler d1 create pratejra-db
# 2. Create Vector index: bun wrangler vectorize create pratejra-index --dimensions=768 --metric=cosine
# 3. Update database_id in wrangler.toml with the ID from step 1
# 4. Run schema: bun wrangler d1 execute pratejra-db --file=db/schema.sql

# =============================================================================
# SECRETS MANAGEMENT
# =============================================================================
# For production deployment, set secrets using:
# bun wrangler secret put LANGFUSE_PUBLIC_KEY
# bun wrangler secret put LANGFUSE_SECRET_KEY
